## ralph/rules-and-about

## Codebase Patterns
- Python version is 3.14.2 (not 3.10.6 as earlier memory noted) — venv uses python3.14
- Build backend: use `setuptools.build_meta` (NOT `setuptools.backends.legacy:build` which doesn't exist)
- Run tests with: `.venv/bin/pytest test_scoring.py -v` (pytest.ini may set rootdir to /home/gustavohssm)
- `yahtzee.egg-info/` is created by `pip install -e .` — don't commit it
- Package `yahtzee` is installed in editable mode with `pip install -e .`

## 2026-02-27 - US-001
- Created `pyproject.toml` with `[project]` (name=yahtzee, version=0.1.0) and `[build-system]` (setuptools.build_meta)
- Ran `pip install -e .` to install the package in editable mode
- Verified `importlib.metadata.version('yahtzee')` returns `0.1.0`
- All 55 existing tests pass
- Files changed: `pyproject.toml` (new)
- **Learnings for future iterations:**
  - `setuptools.backends.legacy:build` does NOT exist — use `setuptools.build_meta`
  - Python venv is 3.14.2, not 3.10 as memory notes suggested
  - `yahtzee.egg-info/` created after install; add to .gitignore eventually
---

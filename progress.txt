## Progress Log — ralph/scoreboard-and-build

## Codebase Patterns
- Project uses `yahtzee/` package for all UI/game components; root-level `.py` files are standalone modules (scoring.py, game.py)
- Textual app is in `yahtzee/app.py`; each modal screen is in its own file under `yahtzee/`
- Tests live at root level: `test_scoring.py`, `test_game.py`
- Run tests: `pytest test_scoring.py test_game.py -v`

## 2026-02-27 - US-001
- Created `yahtzee/scores.py` with `load_scores()`, `save_score(name, score)`, `get_top_10()`
- Scores stored at `~/.local/share/yahtzee/scores.json` as `{"scores": [...]}`
- All stdlib only: `json`, `pathlib`, `datetime`, `sys`
- `load_scores()` returns `[]` on missing file or malformed JSON (prints warning to stderr)
- `save_score()` creates dirs with `mkdir(parents=True, exist_ok=True)`
- **Learnings for future iterations:**
  - The `yahtzee/` package imports `scoring` (root-level module) directly — not `yahtzee.scoring`
  - No typecheck tool (pyright/mypy not installed); "typecheck passes" means no syntax/import errors at runtime
  - Quality check command: `pytest test_scoring.py test_game.py -v`
---

## 2026-02-27 - US-002
- Created `yahtzee/scoreboard_screen.py` with `ScoreboardScreen(ModalScreen)`
- Accepts `mode='postgame'` or `mode='peek'` parameter
- Builds an ASCII box-drawing table (┌─┬─┐ style) via `_build_table()` helper
- Shows "No scores yet!" when `get_top_10()` returns empty list
- In 'peek' mode: `on_key` catches all key events and dismisses with None
- In 'postgame' mode: Enter/N → dismiss('new_game'), Q → dismiss('quit')
- Dark theme: #16213e background, #f0883e accent border, #8b949e hint text
- **Learnings for future iterations:**
  - `on_key` override with `(self, event)` needs `# type: ignore[override]` to silence type warnings; works cleanly at runtime
  - Import scores module as `from yahtzee.scores import get_top_10` (package-relative)
  - Modal width 52 fits the 4-column table comfortably; dialog uses `height: auto`
---

## 2026-02-27 - US-003
- Created `yahtzee/name_entry_screen.py` with `NameEntryScreen(ModalScreen)`
- Modal shows title 'Game Over — Enter your name' and a Textual `Input` widget (max_length=20)
- `on_input_submitted` handler strips whitespace and defaults to 'Anonymous' if empty, then calls `dismiss(name)`
- `on_mount` focuses the Input widget so player can type immediately
- Dark theme: #16213e background, #f0883e accent border, #8b949e hint text
- **Learnings for future iterations:**
  - Textual `Input.Submitted` event fires when Enter is pressed; access value via `event.value`
  - `Input` widget imported from `textual.widgets`; `placeholder` and `max_length` are constructor params
  - `on_input_submitted(self, event: Input.Submitted)` is the correct handler signature
---

## 2026-02-27 - US-004
- Modified `yahtzee/app.py` to wire GameOverScreen → NameEntryScreen → ScoreboardScreen
- Added imports: `NameEntryScreen`, `ScoreboardScreen`, `save_score`
- `_handle_game_over` now pushes `NameEntryScreen` (ignores GameOverScreen result — decision deferred to scoreboard)
- `_handle_name_entry` calls `save_score(name, self.game.grand_total)` then pushes `ScoreboardScreen(mode='postgame')`
- `_handle_scoreboard` handles 'new_game' → `_reset_game()` and 'quit' → `self.exit()`
- **Learnings for future iterations:**
  - Textual `push_screen(screen, callback)` chains modals cleanly; each callback receives the dismissed value
  - The GameOverScreen result is discarded in the new flow — all routing decisions moved to ScoreboardScreen callback
  - `save_score` accepts `str | None`; guard with `name or "Anonymous"` before calling
---

## 2026-02-27 - US-005
- Modified `yahtzee/app.py`: added `('s', 'show_scoreboard', 'Scores')` to `BINDINGS`
- Added `action_show_scoreboard()` that pushes `ScoreboardScreen(mode='peek')`
- No changes to `ScoreboardScreen` — 'peek' mode already handles any-key dismiss with `None` (from US-002)
- S binding appears in Textual footer alongside R, N, Q
- **Learnings for future iterations:**
  - Adding a binding only requires two changes: a tuple in `BINDINGS` and a matching `action_<name>()` method
  - `ScoreboardScreen(mode='peek')` already returns state unchanged — no callback needed for peek
---

## 2026-02-27 - US-006
- Updated `yahtzee/__main__.py`: added `def run(): YahtzeeApp().run()` and wrapped module-level execution in `if __name__ == "__main__": run()`
- Created `pyproject.toml` at project root with `[project]` and `[project.scripts]` sections
- `yahtzee.__main__:run` is the declared entry point for the `yahtzee` console script
- **Learnings for future iterations:**
  - Previous `__main__.py` had bare module-level `app = YahtzeeApp(); app.run()` — no guard; replaced with `run()` + `if __name__ == "__main__":`
  - `pyproject.toml` uses `[project.scripts]` (not `[tool.poetry.scripts]`) for the standard entry point declaration
  - `python -m yahtzee` still works because `__main__.py` is present in the package
---

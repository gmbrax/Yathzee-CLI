## ralph/rules-and-about

## Codebase Patterns
- Python version is 3.14.2 (not 3.10.6 as earlier memory noted) — venv uses python3.14
- Build backend: use `setuptools.build_meta` (NOT `setuptools.backends.legacy:build` which doesn't exist)
- Run tests with: `.venv/bin/pytest test_scoring.py -v` (pytest.ini may set rootdir to /home/gustavohssm)
- `yahtzee.egg-info/` is created by `pip install -e .` — don't commit it
- Package `yahtzee` is installed in editable mode with `pip install -e .`

## 2026-02-27 - US-001
- Created `pyproject.toml` with `[project]` (name=yahtzee, version=0.1.0) and `[build-system]` (setuptools.build_meta)
- Ran `pip install -e .` to install the package in editable mode
- Verified `importlib.metadata.version('yahtzee')` returns `0.1.0`
- All 55 existing tests pass
- Files changed: `pyproject.toml` (new)
- **Learnings for future iterations:**
  - `setuptools.backends.legacy:build` does NOT exist — use `setuptools.build_meta`
  - Python venv is 3.14.2, not 3.10 as memory notes suggested
  - `yahtzee.egg-info/` created after install; add to .gitignore eventually
---

## 2026-02-27 - US-002
- Created `yahtzee/assets/` directory and `yahtzee/assets/rules.md` with full Yahtzee rules
- Sections included: Objective, Turn Flow, Upper Section (with Bonus), Lower Section (all 7 categories), Scoring a Zero
- File is well-formed Markdown with headings, lists, and bold point values — no HTML tags
- Verified loadable by `textual.widgets.Markdown` (imported and read without errors)
- All 55 existing tests continue to pass
- Files changed: `yahtzee/assets/rules.md` (new)
- **Learnings for future iterations:**
  - Assets live at `yahtzee/assets/` — use `Path(__file__).parent / 'assets' / 'filename'` to load from screen modules
  - Textual's Markdown widget loads raw markdown text via `.update(content)` or constructor; verify by reading the file in Python
---

## 2026-02-27 - US-004
- Added `from yahtzee.rules_screen import RulesScreen` to `yahtzee/app.py`
- Added `("question_mark", "show_rules", "Rules")` to `BINDINGS` in `YahtzeeApp`
- Added `action_show_rules()` with guard `if len(self.screen_stack) == 1`
- All 55 existing tests continue to pass
- Files changed: `yahtzee/app.py` (modified)
- **Learnings for future iterations:**
  - Textual binding key name for `?` is `"question_mark"` (not `"?"`); same pattern applies to other special chars
  - Guard pattern `if len(self.screen_stack) == 1` prevents double-pushing modals; screen_stack[0] is always the base screen
---

## 2026-02-27 - US-003
- Created `yahtzee/rules_screen.py` with `RulesScreen(ModalScreen)`
- Used `VerticalScroll` wrapping `Markdown` widget for scrollable content
- Loads `yahtzee/assets/rules.md` at compose time via `Path(__file__).parent / 'assets' / 'rules.md'`
- Dialog is 80% wide × 90% tall, centered, with round green (#3fb950) border and #16213e background
- Hint line at bottom: `  ↑/↓ scroll  ·  Esc or ? close  ` in #8b949e
- Both Escape and question_mark keys dismiss via `on_key` handler
- CSS is inline on the class — no separate .tcss file
- All 55 existing tests continue to pass
- Files changed: `yahtzee/rules_screen.py` (new)
- **Learnings for future iterations:**
  - Modal screen pattern: `align: center middle` on the screen class, then a single container div with explicit width/height and border
  - `VerticalScroll` is available in Textual 8.0.0 (no need for `ScrollableContainer`)
  - Use `height: 1fr` on the scroll container and `height: auto` on the hint bar to fill remaining space
  - For key names in `on_key`: use `"escape"` and `"question_mark"` (not `"?"`)
---
